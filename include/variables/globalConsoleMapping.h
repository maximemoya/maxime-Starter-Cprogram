#ifndef GLOBAL_CONSOLE_MAPPING_H
#define GLOBAL_CONSOLE_MAPPING_H

// The global_console_mapping[] extern and its MapInput / MappingLabel types live in
// input_mapping.h; this header exists so consumers can include it by the variables/ path.
#include "game/core/event_handler/input_mapping/input_mapping.h"

#endif // GLOBAL_CONSOLE_MAPPING_H
