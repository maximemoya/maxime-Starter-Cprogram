# distribution binaries and app
dist

# Temporary Dockerfile generated by makefile.rg35xxh (setup-docker-image).
# Removed on the success path; ignored so a failed build never commits it.
Dockerfile.tmp