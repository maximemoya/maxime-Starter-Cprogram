# For macOS ARM Mx:
# make -f makefile.mac run

# For Linux AMDx64 / Ubuntu (incl. WSL2, native x86_64 build):
# make -f makefile.linux run

# For RG35XXH (muOS):
# make -f makefile.rg35xxh docker-build